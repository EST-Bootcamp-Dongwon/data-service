# NOTICE — data-service

## 원저작자 표시

이 모듈은 아래 저장소의 코드를 일부 이식·개작했다.
원저작자 표시는 **유지 조건**이며 지우지 않는다.

| 원본 | 이식 대상 | 라이선스 |
|---|---|---|
| ④ `projects/data-service` | `clients/*` · `core/trading_calendar.py` · `core/parallel.py` · `repositories/*` | **MIT** (원문 확인 필요) |
| ② `Research-Prompt-Engineering` | `gic_v14/opendart/*` · `normalize/facts.py` | 명시 없음 → 원저작자 표시 유지 |
| ③ `projects/stock-coin-trade` | `market/quotes.py` · `sessions.py` | 강사님(edumgt) 원본 fork |

## 제3자 라이선스

의존성을 추가할 때마다 이 절에 추가한다. **라이선스명만 적지 말고 금지 행위를 한 문장으로 적는다.**

| 대상 | 라이선스 | 이 라이선스가 금지하는 것 |
|---|---|---|
| (예) vectorbt | Apache 2.0 **+ Commons Clause** | 이 소프트웨어가 곧 제품인 상품·서비스를 **판매**하는 것 |

> ⚠️ vectorbt는 OSI 오픈소스가 아니다. `quant-core`(pip 배포물)에는 절대 넣지 않는다.
