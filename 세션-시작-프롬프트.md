# 세션 시작 프롬프트 — data-service

> 새 Claude Code 세션을 `projects\data-service` 에서 열고 **이 파일 전체를 첫 메시지로 붙여넣으세요.**
> 리서치 결과가 갱신되면 이 파일도 함께 갱신합니다.
> 최종 갱신: 2026-08-25 — ⭐ **배포본이 DB 를 읽는다**(**ADR-DS-0021** · 전환 **S6**).
> Supabase 에 core 350종목 103,663행이 서 있고, **실제 프로덕션에서** 화면 11 + API 9 가
> **전부 200 · `mode=db` · `source=krx-db`** 다(배포 전에는 `mode=live` · 0행).
> 잠겨 있던 셋(시세·보관함·수집)이 한꺼번에 풀렸다.
> ⭐ **`universe_tier` 가 처음으로 참이다** — KRX 정보데이터시스템에서 KOSPI200+KOSDAQ150
> 실제 구성종목을 받는다(core 350 / full 2,525). ADR-DS-0014 가 비워 두고 ADR-DS-0020 이
> 시총 상위 N 으로 때우던 자리다. ⚠️ **보유한 KRX OpenAPI 키로는 안 된다** — 구성종목
> 서비스가 스펙에 없다. 계정(`KRX_ID`·`KRX_PW`)이 따로 필요하다.
> 그 앞에 **자동 수집이 돌기 시작했다**(**ADR-DS-0020**) ·
> **자료 보관이 동작한다**(**ADR-DS-0019**) ·
> **로컬 읽기 경로가 Postgres 다**(**ADR-DS-0018** · 전환 **S5**).
> SQLite 파일을 치우고도 화면 10개 + API 18경로가 전부 200 이고 층 표시가 전부 `krx-db` 다.
> 기본값은 이제 **두 환경 다 `postgres`** 다(S6 이 배포본을 뒤집었다).
> ⚠️ **순서가 뜻을 가진다** — Vercel `DATABASE_URL` 이 먼저, 기본값을 뒤집은 push 가 나중.
> ⚠️ 뒤집자 그 값이 **갱신 사슬 자식까지 새어 들었고**, 스냅샷이 직전 회차 자료로 조용히
> 만들어질 뻔했다. 사슬은 쓰기 측(SQLite)에 못 박혀 있다(`refresh_job.CHAIN_ENV`).
> DDL 만 서 있던 `clip` 표가 실제로 쓰인다 — `/stock` 에서 담기·목록·필터·산업 자동유도.
> **DART 공시·정기보고서가 자동으로 담긴다** — 시총 상위 350종목 회차 실측 **16,286건 · 209초 ·
> 실패 0**, 다시 돌려도 새로 담기는 것이 거의 없다(멱등). 갱신 사슬에는 **얹지 않았다** —
> 시세(SQLite)와 보관함(Postgres)은 축이 다르다. `refresh_job.py` 는 한 줄도 안 바뀌었다.
> ✅ **S6 이 그 잠금을 풀었다.** ⚠️ 다만 배포본의 `clip` 은 **비어 있다** — 표는 섰고
> 잠금은 풀렸지만 `security_id` 가 양쪽에서 다르게 매겨져 종목코드로 다시 짝지어야 한다.
> **다음은 ① 배포본 clip 시딩 · ② 네이버 뉴스**(일일 쿼터 상한 설계가 먼저) **· ③ S7**.
> 앞선 것: 화면 갱신(ADR-DS-0017) · S4 읽기 어댑터(ADR-DS-0015) · 갱신 명령(ADR-DS-0016) ·
> S3 적재기(ADR-DS-0014) · 정체성·정보구조(ADR-DS-0012) · 문서 볼트 미러(ADR-DS-0013) ·
> S2 엔진 계층 · ADR-DS-0003 rev.2(커넥션 전략 실측으로 닫음).

---

## 0. 함께 첨부할 것

새 세션에는 **이 파일 하나만** 첨부한다.

⚠️ 예전에는 상위 문서(`quant-contract`의 `CONTEXT.md`·`09-프로젝트-구조-재설계.md`)를 함께
첨부했다. 그 문서들은 **모듈 분해를 전제로** 쓰여 있었는데 이 레포의 방침은 처음부터
달랐다(§2.1). `quant-contract` 저장소 자체가 2026-09-15 에 폐기됐으므로 이제 첨부할
상위 문서가 없다 — 이 파일이 정본이다.

## 1. 지금 여는 폴더

```
C:\Users\kik32\workspace\EST-Camp-AI-Quant\projects\data-service
```

✅ 이 폴더는 **git 서브모듈이자 독립 저장소**다. `api-test` 를 `git mv` 로 개명해 승격했으므로
히스토리와 원격이 그대로 이어진다. **호스트 개명은 완료됐다.**

| 원격 | 저장소 |
|---|---|
| `origin` | `github.com/EST-Bootcamp-Dongwon/data-service` |
| `gitlab` | `gitlab.com/mygithub05253/est-data-service` |

> GitLab 쪽만 `est-` 접두사가 붙어 있다(경로 규칙). **이름 불일치는 버그가 아니다.**
> ⚠️ **GitLab 원격에 Vercel 이 연결돼 있다. push 가 곧 배포다.**

## 2. 이 모듈이 무엇인가

> **흩어져 있는 국내 투자 정보를 한 곳에 모아 두는 수집·보관·조회 서비스.**
> 시세·통계는 정형 API 로, 공시·뉴스·커뮤니티는 수집으로 모으고, **기업과 산업**을 축으로 되찾는다.

수집 대상은 **여섯 갈래**다 — 공시 · 정기보고서 · 뉴스 · 커뮤니티 · 동영상 · 시세/통계
(ADR-DS-0012). 지금 도는 것은 **시세·통계 9원천뿐**이고 나머지 다섯은 아직 없다.
⚠️ 이 문장은 **네 곳이 공유한다**(README·랜딩·`/guide`·OpenAPI). 넷이 어긋나면 그것이 결함이다.

나머지 모듈이 전부 이걸 쓴다.

**이 레포는 ④ `api-test`를 개명해 승격한 것이다**(2026-08-16, `git mv`). 복사본이 아니라
같은 저장소이고 히스토리가 이어진다. **코드는 이미 여기 있다. 옮겨올 것이 없다.**

실측 규모 (2026-08-18):

| | 실물 |
|---|---|
| 클라이언트 | **9종** — dart_data · dart_report · ecos_data · fred_data · fss_data · hf_data · kosis_data · krx_data · yf_data |
| 라우터 | **17파일** (API 15 + 화면 1 + `__init__`) · **엔드포인트 64개** |
| 리서치 하네스 | `app/services/research/` **24개 파일**(`__init__` 3개 포함, 실질 21개) · 약 11,241행 |
| 시계열 | `app/services/timeseries/` 9파일 · 2,052행 |
| 저장계층 | **SQLite 3개 172MB** + JSON + `/tmp` + 인메모리 **4갈래.** 전환은 S2/9 진행 중 |
| 거래일 | `core/trading_calendar.py` — **공휴일을 모른다.** `weekday() < 5` 로 주말만 거른다 |

**신규 개발이 아니라 계약(OpenAPI)을 정리하고 저장 계층을 붙이는 작업이다.**

### 2.1 순서를 헷갈리지 말 것 — 지금은 쪼개지 않는다

**개인 프로젝트를 먼저 완성하고, 그 다음 코드를 가져가 팀 프로젝트로 발전시킨다.** 반대가 아니다.
그래서 지금은 **모듈을 쪼개지 않는다.** `services/research/*` · `services/timeseries/*` ·
`static/` 화면 10개 — 전부 이 레포에 남는다. 나중에 꺼내기 쉽도록 **경계만 긋는다**(ADR-DS-0007).

⚠️ **팀 프로젝트는 별도 레포이고, 이 레포는 개인 프로젝트로 완성한다** (ADR-DS-0012 §9 ·
2026-08-25 사용자 확인). 팀 쪽은 **필요한 코드만 가져간다**. 어느 정도 완성되면 그때
이쪽으로 되가져와 반영할 수 있지만, **그것을 근거로 지금 범위를 늘리거나 팀 협업 장치
(브랜치 전략·이슈 템플릿·다인 배포)를 미리 넣지 않는다.**
팀 프로젝트 자체는 **2주 × 3개 = 6주 점진 확장**이다 — 그 사실은 문서로 남겨 두되
이 레포의 작업 순서를 그쪽에 맞추지 않는다.

### 2.2 이 레포의 경계

**데이터를 가져오는 것이 본업이다.** 리포트 생성 파이프라인은 `research-service` 로 옮길
계획이었으나, 그 모듈을 개발하지 않기로 해(2026-09-15) **옮기지 않고 이 저장소에 남는다.**
경계표는 그 ADR 안에 있다 (ADR-DS-0007). ⚠️ **지우지 않는다.**

## 3. 절대 제약 — 이걸 어기는 제안은 하지 말 것

| # | 제약 | 근거 |
|---|---|---|
| 1 | **LLM 유료 API 비용 0원** | 강사님 방침 |
| 2 | **매매 신호 생성 경로에 LLM 금지** | 비결정성 · 재현 불가 · 사전학습 룩어헤드 오염 |
| 3 | RAG·Agent는 신호 경로와 **물리적으로 분리된 별도 서비스** | 2번의 귀결 |
| 4 | AWS 등 클라우드 인프라 미사용 | 강사님 방침 |
| 5 | Docker 이미지 용량 최소화 | 로컬 디스크 제약 |
| 6 | **CI/CD를 필수 경로에 두지 않음** | GitHub 계정 리스크 |
| 7 | **헤비 프론트 프레임워크 금지** (Next.js·React SPA·번들러) | 강사님 방침. 빌드 단계가 생기면 6번도 흔들림 |

> HuggingFace **인코더** 호출은 유지한다 — 생성형 LLM 이 아니다(감성·제로샷·임베딩 3종).

## 4. 이미 확정된 것 — 다시 논의하지 말 것

- **저장소 이원화** — 로컬 Postgres가 정본(full), Supabase는 데모용(core) (ADR-CT-0007)
- **유니버스 2단계** — `core` = KOSPI200+KOSDAQ150(약 350) / `full` = 전종목+상장폐지 (ADR-CT-0010)
- **OHLC는 `integer`**, `ohlcv`는 연 단위 RANGE 파티셔닝
- **엔트리포인트 정본 `app.main:app`**, 루트 `main.py` 는 1줄 shim (ADR-DS-0006)
- **`ruff format` 을 검증 경로에 넣지 않는다** (ADR-DS-0005). lint(`ruff check`)만 게이트다
- **`pgvector/pgvector:0.8.2-pg16`** (CVE-2026-3172 패치본). `pg16` 롤링 태그 금지
- **`asyncpg==0.30.0` 을 유지한다.** 0.31.0 은 `manylinux_2_17` 휠을 내지 않아(2_28 만)
  Vercel 빌드가 **조용히** 깨질 수 있다. 올리려면 ADR-DS-0003 rev.2 의 실측표를 다시 잰다
- **DB 컨테이너는 compose profile `local-db` 뒤**에 둔다. 기본 up에서는 안 뜬다
- **의존성 정본은 `pyproject.toml` + `uv.lock`.** `requirements.txt`는 `uv export` 생성물
- **응답 상한** (ADR-DS-0004, 2026-08-17 완료) — FRED `max_points=2000` · 야후·차트 `3000`.
  `yf_data.MAX_HISTORY_ROWS` 와 `market_chart.MAX_POINTS` 는 **같은 값을 유지한다**(테스트가 검사).
  `market_chart.series` 는 야후를 `max_rows=0` 으로 불러 **이동평균을 전 구간에서 계산한 뒤 자른다**
- **`/api/research/export/*` 를 환경으로 막지 않는다** (ADR-DS-0004). 화면이 쓰는 리포트 렌더러다
- **리서치 파이프라인은 경계만 긋고 이관은 보류** (ADR-DS-0007)
- **자료 보관은 한 표 `clip` + `kind`** (ADR-DS-0008). 날짜는 `occurred_at`(자료 날짜)과
  `saved_at`(담은 시각) **두 축**. 뉴스 본문은 담지 않는다(링크·제목·출처·발행일·메모까지).
  산업은 종목에서 자동 유도 + 수동 보정(`industry_source`). 스냅샷은 조건 + 요약통계까지만
- Vercel 실행시간은 Fluid compute **전 플랜 300초**, 요청·응답 본문 **4.5MB 한도**
- **배포 방침 (2026-08-18 확정)** — `krx_bundle.db`·`report_index.db` 를 git 에 넣지 않는다.
  배포본에서 `/quant`·`/krx` 캔들과 자연어 검색이 강등되는 것을 **감수하고, ② 저장계층 전환으로 해결한다**

## 5. legacy — 읽기 전용

### 승격된 것 — 더 이상 legacy 가 아니다 (2026-08-16)

| 기호 | 이전 이름 | 현재 |
|---|---|---|
| ④ | `api-test` | **`projects/data-service`** |
| ① | `investment-portfolio-site` | **`projects/investment-dashboard`** |

### 읽기 전용 — 수정 금지

코드를 옮길 때는 이 모듈 안에 **복사한 뒤** 수정한다.

| 기호 | 경로 | 스택 | 계보 |
|---|---|---|---|
| ② | `C:\Users\kik32\workspace\Research-Prompt-Engineering` | Python | **projects 밖!** |
| ③ | `projects/stock-coin-trade` | Django 5.2.17 + DRF 3.18 + HTMX/Alpine | `upstream` + `upstream-main` |
| ⑤ | `projects/docker-class` | Docker·DevSecOps 실습 | `upstream` + `upstream-main` |
| — | `projects/investment-analysis` | 강의 원본 | `upstream` |

**③⑤와 investment-analysis 는 폴더를 이동·개명하지 않는다.** 3-way merge 계보가 살아 있다.
각 레포의 `NOTICE.md`(원저작자 edumgt 표시)는 승격 후에도 유지한다.

## 6. 끝난 것 / 남은 것

### 끝난 것 — 전부 `invoke check` 통과 (**391 tests** · 이미지 553MB)

| 영역 | 산출물 |
|---|---|
| 의존성 정본 | `pyproject.toml` + `uv.lock`. `requirements.txt` 는 `uv export` 생성물 |
| 검증 정본 | `tasks.py` (`invoke check` = **preflight** → ruff check → pytest → uv export → docker build) |
| 회귀 안전망 | `tests/` — 계약 스냅샷 + 정적 마운트 + 엔트리포인트 동일성 + 상한 12종 |
| 엔트리포인트 | `app.main:app` 정본 + 루트 `main.py` shim + `app/core/paths.py` (ADR-DS-0006) |
| 컨테이너 | `Dockerfile` · **`compose.yaml`** · `.dockerignore` — 553MB, `up -d` 후 healthy. 프로파일 셋: 기본 · `local-db`(5432) · **`pooler`(6543 transaction)** |
| 저장계층 DDL | `sql/init/` **6표** + 14파티션. **DDL 만 섰고 코드는 아직 SQLite** |
| 자료 보관 | ADR-DS-0008. `clip` 표 DDL + 제약 3종·인덱스 7개. 빈 볼륨 기동으로 검증 완료 |
| 응답 상한 | ADR-DS-0004 완료. 시계열 5경로 전부 상한 + `truncated`·`total_count` |
| 리서치 경계 | ADR-DS-0007. 경계표만 작성, 이관은 보류 |
| 출처 어휘 | ADR-DS-0009. `<provider>-<tier>` 두 토막. `tests/test_source_vocabulary.py` 가 화면까지 검사 |
| 환경 진입점 | ADR-DS-0003 **구현 완료**. `app/core/settings.py` + `APP_ENV` 분기 |
| 전환 S1 | `scripts/check_migration_fitness.py`(읽기 전용 자) + `tests/test_schema_fitness.py` 17개. `change_rate` `numeric(8,4)`→`(12,4)` (실측 29948.08) |
| 시점 주식수 | ADR-DS-0010. `ohlcv.listed_shares` 복원. 접으면 액면분할 종목 회전율이 10배 틀린다 |
| 전환 순서 | **ADR-DS-0011** — S1~S9 아홉 걸음. 걸음마다 되돌릴 수 있어야 한다는 것이 원칙 |
| 전환 S2 | **엔진 계층 `app/core/db.py`** + `sqlalchemy[asyncio]==2.0.52`·`asyncpg==0.30.0`. `tests/test_db.py` 52개 · `scripts/check_db_connection.py`(읽기 전용 실측기). **아직 아무도 import 안 한다** |
| 커넥션 실측 | **ADR-DS-0003 rev.2** — pgbouncer 1.25.2 transaction 모드 실측. **결정이 바뀌었다**: 배포본 손잡이가 셋 → **넷**(캐시 둘 + `NullPool` + **준비구문 이름 유일화**). 캐시만 끈 전략은 갓 띄운 풀러에서 0건이다가 잔여물이 쌓이면 **첫 질의부터 100% 실패**한다 |
| 검사 선행 | `invoke preflight` — venv 가 pyproject 를 따라잡았는지 본다(`check` 가 먼저 부른다) |
| **정체성·정보구조** | **ADR-DS-0012** — 수집 6갈래 · 화면을 자료 종류로 가름 · 랜딩 순서 뒤집기. 사이드바 `NAV` 6묶음 재편(수집 자료 5개는 `soon`) · 랜딩 재구성 · README 구성 재작성 · `/guide`·OpenAPI 정체성 일치 |
| **문서 볼트 미러** | **ADR-DS-0013** — `scripts/sync_obsidian.py` + `post-commit` 훅(`invoke hooks`). `Master_Obsidian/30_Projects/data-service/` 로 **한 방향**. 17문서 + 인덱스 노트 |
| **전환 S3** | **일회성 적재기 `scripts/load_pg.py`** + `tests/test_load_pg.py` 36개. 780,484행 → `ohlcv`+`securities`, `fetch_log` 300행 → `ohlcv_sync_log`(rows=0 이 18 · `status='empty'`). **실측 32초 · 대조 21항목 전부 일치 · 재실행 안전.** `clip_kind_ck` 를 적재 전에 일곱 값으로 넓혔다 |
| **S3 결정** | **ADR-DS-0014** — S1 이 "판단 대기"로 미뤄 둔 셋(`securities` 원천 · `name` 이력 · `is_delisted`)에 답했다. **추정을 사실로 굳히지 않는다**가 축이다 |
| **전환 S4** | **읽기 어댑터 `app/repositories/krx_pg.py`** + `STORE_BACKEND` 스위치(`settings.py` §3) + **동기 다리**(`db.py` §2-1). `tests/test_krx_pg.py` 42개 + 다리 검사 9개. 함수 24항목 중 23 일치 · HTTP 20경로 중 19 완전 일치 · 48 동시 요청 48/48. **기본은 `sqlite` 라 배포본은 그대로다** |
| **S4 결정** | **ADR-DS-0015** — 호출마다 `asyncio.run()` 을 **실측으로 기각**(로컬 풀에서 48회 중 15회 실패). 전용 배경 루프 하나로 두 커넥션 전략 모두에서 돈다 |
| **검사 격리** | `tests/conftest.py` autouse `isolate_env` — 개발자 셸의 `DATABASE_URL` 이 검사 안까지 새어 들던 구멍을 막았다. 오염된 환경에서 돌려도 같은 결과 |
| **자료 갱신** | **ADR-DS-0016** — 갱신 사슬 다섯 단계를 `invoke refresh` 로 묶었다. `invoke check` 에는 **묶지 않는다**(외부 API·파일 변경). 2026-08-01 이후 24일 밀렸던 시세를 따라잡혔다 — 캐시 **821,928행**(20260824) · 스냅샷 **2026-08-24 최신** · Postgres 재적재 대조 21항목 일치. KRX 카드가 `APP_ENV` 별로 처방을 가른다 |
| **화면 갱신** | **ADR-DS-0017** — 갱신 실행처를 *사람 + 화면*으로 확정. 사슬 정본을 `app/services/refresh_job.py` 의 `STEPS` 하나로 모으고 `tasks.py` 는 그것을 부른다. `app/routers/refresh_router.py` 3경로 + 대시보드 '자료 갱신' 패널 + `settings.refresh_api()` 손잡이. `tests/test_refresh_job.py` **36개**. 실측 — HTTP 로 전 구간 **270.5초** · `--check` 43.4초 · 겹쳐 실행 `409` · 배포본 `503` · 중단 정상. 갱신 뒤 **프로세스 사본 다섯**을 씻는다(가장 조용한 고장 지점) |
| **전환 S5** | **로컬 읽기 기본값을 `postgres` 로.** 아홉 번째 이음매 `krx_store.lookup_security()` 로 `stock_service` 우회를 걷어냈고, 기본값을 **환경별**로 두었다(로컬 `postgres` · 배포본 `sqlite`). 갱신 사슬은 쓰기 측에 못 박았다(`refresh_job.CHAIN_ENV`). 검사 격리에 `STORE_BACKEND=sqlite` 한 줄이 더 필요해졌다. **SQLite 파일을 치우고 화면 10개 + API 18경로 전부 200 · 층 표시 전부 `krx-db` · 폴백 0건** |
| **S5 결정** | **ADR-DS-0018** — 우회 둘 중 하나는 **빚이 아니었다**(`build_stock_master.py` 는 사슬 4단계라 Postgres 를 읽으면 직전 회차 자료를 쓰고 `--skip-pg` 가 깨진다). 실측: `lookup_security` 두 저장소 **20/20 일치** · 이름 조회 SQLite 3~7.6초 vs Postgres 2~7ms · 동시 48요청 **Postgres 48/48·8.9초 vs SQLite 24/48·121초** |
| **자료 보관** | **ADR-DS-0019** — `clip_store.py` + `clip_router.py`(7연산) + `/stock` 패널(담기·목록·필터·삭제). `kind` 별 검증은 라우터에, DDL 이 거는 것은 표에 — `tests/test_clip.py` 가 **DDL 파일을 읽어** 어휘를 대조한다. 중복 링크는 오류가 아니라 기존 것을 돌려준다. 실측 — 저장소 13항목 · API 검증 7건 · jsdom 으로 `/stock` 실주행 |
| **자동 수집** | **ADR-DS-0020** — `dart_collector.py`(정본) + `watermark_store.py` + `collect_router.py`(2경로) + `scripts/collect_dart.py` + `invoke collect` + `/stock` 버튼 + 대시보드 카드. `tests/test_dart_collect.py` **82개** + `regress_ui.js` 7절 11개. **적대적 검증 29건 중 21건 확정 → 전부 수리**(그중 하나가 9종목 자료 손실이었다). 실측 — 상위 350종목 **16,286건 · 209초 · 1,039호출 · 실패 0**, 재실행 새로 47·이미 16,482(멱등). **갱신 사슬 무변경**(`test_refresh_job.py` 39건 그대로) |
| **전환 S6** | ⭐ **배포본이 DB 를 읽는다.** Supabase `data-service`(ap-northeast-2 · free) 에 스키마를 올리고 **카탈로그 지문 넷**(컬럼 226·제약 43·인덱스 46·파티션 14)으로 로컬과 대조 — 전부 일치(로컬 pg16 vs Supabase pg17.6). core **103,663행 · 25초** 적재 · 대조 21항목 일치. 커넥션 (a)(b)(c) 를 **실제 Supavisor** 에 돌려 통과. 배포본 환경 스모크 **24/24 · `mode=db` · 보관함·수집 `available:true`**. `DEFAULT_STORE_BACKEND_VERCEL` 을 `postgres` 로 |
| **core 유니버스** | ⭐ **`data/universe_core.json` 350종목**(KOSPI200 200 + KOSDAQ150 150) + `scripts/build_universe.py`. `universe_tier` 가 처음으로 참(core 350 / full 2,525). `load_pg.py --universe {full,core}` + 원격 가드 둘로 분리. pykrx 를 **의존성으로 들이지 않고** 프로토콜만 옮겼다 |
| **S6 결정** | **ADR-DS-0021** — 실측표로 경로를 갈랐다(KRX OpenAPI 구성종목 **없음** · 네이버 `robots.txt` **금지** · 정보데이터시스템 **계정 필요**). 시총 대용을 `core` 로 찍지 않는다 — **컬럼이 거짓말을 한다** |
| **문서 게이트** | `.markdownlint-cli2.jsonc` — **6,500건 → 0건.** `invoke docs-check` 가 처음으로 완전히 초록이다. lychee 부재 시 상대 링크 폴백(`tasks.py:_check_local_links`) |

> ⚠️ 파일명은 `docker-compose.yml` 이 아니라 **`compose.yaml`** 이다 (Compose Spec 권장명).

### 남은 작업 — 권장 순서

**① 랜딩·정체성** ✅ **끝났다** (2026-08-23 · ADR-DS-0012)
   사용자가 범위를 정했다: 퀀트 투자 시스템을 위해 **뉴스·공시·분기/반기보고서·커뮤니티·
   동영상(링크)** 까지 거의 모든 정보를 크롤링하고, **데이터 종류별 화면** + **기업·산업 검색**,
   **날짜/월/연·기업·산업 필터**를 둔다. 여러 크롤링 사이트를 **아우르는 하나의 사이트**다.
   - 사이드바 6묶음 재편 — '수집 자료' 5개는 `soon` 으로 걸어 두었다(감추지 않는다)
   - 랜딩 순서를 뒤집었다 — 수집 갈래 → 데이터 상태 → 시장 카드 → 시장 온도 → 리서치(빈 자리)
   - 정체성 한 문장을 **네 곳**(README·랜딩·`/guide`·OpenAPI)이 공유한다
   - ⚠️ **남은 것 하나 — README 이미지**(강사님 요건 4번). 이 환경에 브라우저·스크린샷 도구가
     **없어서 못 찍었다.** 사용자가 직접 캡처해 `docs/images/` 에 넣으면 README 에 건다.

**② 저장계층 실제 전환** (ADR-DS-0002 실행 · 순서는 **ADR-DS-0011**) — **S1~S6 완료, S7 부터**
   걸음과 완료 조건은 ADR-DS-0011 의 표가 정본이다. 다음은 **S7 — `bundle`·`snapshot` 폐기**다.
   - ✅ **S6 은 끝났다** (ADR-DS-0021). 배포본이 Supabase 를 읽고 `mode=db` 다.
     Supabase 프로젝트 `data-service` · ap-northeast-2 · free · core 350종목.
     ⚠️ 프로젝트 ref·비밀번호는 **커밋하지 않는다** — 이 레포는 Public 이다.
     ref 는 대시보드에서, 비밀번호는 `.key` 의 `SUPABASE_DB_PASSWORD` 에서 본다.
   - ⚠️ **S7 은 "폐기" 가 곧 "기능 축소" 인 구간이다** — 배포본에는 core 350 만 있으므로
     번들을 버리면 나머지 2,525종목이 갈 곳이 없어진다. 그 사실을 먼저 정하고 시작한다.
   - **S5 는 끝났다** (ADR-DS-0018). 로컬 읽기 기본값이 `postgres` 이고, SQLite 파일을
     치운 채로 화면 10개 + API 18경로가 전부 200 이며 층 표시가 전부 `krx-db` 다
   - **S6 이 한 것 셋** (전부 완료):
     ① Supabase 프로젝트 + core 350종목 적재 (103,663행 · 대조 21항목 일치)
     ② Vercel 환경변수 `DATABASE_URL`(**포트 6543 · transaction 풀러**)
     ③ `settings.DEFAULT_STORE_BACKEND_VERCEL` 을 `postgres` 로
   - ✅ **커넥션 손잡이 넷이 실제 Supavisor 에서 확인됐다** (ADR-DS-0003 rev.2 · (a)(b)(c)).
     그전까지는 로컬 pgbouncer 실측이었다 — 표명이 실물로 닫혔다
   - ⚠️ **직결 주소는 못 쓴다** — `db.<ref>.supabase.co` 는 IPv6 전용이다. 6543 풀러만 IPv4
   - ⚠️ **두 저장소는 갈린다.** 로컬에 새 거래일이 들어와도 Supabase 는 그대로다 —
     `load_pg.py --universe core --allow-remote` 를 다시 돌려야 하는데 **어디에도 안 묶여 있다**
   - ⚠️ `load_pg.py` 는 **원격 DB 를 호스트로 막는다.** 뚫으려면 `--allow-remote`,
     그리고 원격에 `full` 을 부으려 하면 **따로 막는다**(`--universe core` 를 요구)
   - 실측 참고 (2026-08-25 · 로컬):
     동시 48요청 **Postgres 48/48 · 8.9초** vs **SQLite 24/48 · 121초**(절반이 120초 타임아웃).
     이름 조회 **SQLite 3,000~7,600ms vs Postgres 2~7ms** (`daily_price.name` 에 인덱스가 없다).
     ⚠️ 다리의 **루프는 여전히 하나다.** 48 동시에서 견디지만 직렬화는 실재한다 —
     늘리려면 커넥션 전략(rev.2)을 다시 재야 하므로 **근거가 생겼을 때만** 손댄다
   - 뒤로 미룬 것: `tier()` 3단 분기 + 낱말 정리(S7 — 카드 문구 "원본 캐시"→"원본 저장소"
     까지만 했다) · `market_snapshot` 파생 지표 재계산(S9) ·
     `trading_calendar` 채우기·읽기(S9 — `ohlcv_sync_log` 의 rows=0 20일이 이미 알고 있다)
   - ⚠️ **쓰기는 아직 SQLite 다(S8).** 그래서 갱신 사슬이 `refresh_job.CHAIN_ENV` 로
     쓰기 측에 못 박혀 있다. **S8 이 그 못을 뺀다** — 그전에 빼면 스냅샷이 직전 회차
     자료로 조용히 만들어진다

**②-1 갱신 실행처** ✅ **끝났다** (2026-08-25 · ADR-DS-0017)
   사용자가 정했다 — *"실행처가 사람이라면 실제 웹 화면 내에 갱신 버튼을 만들자."*
   대시보드 '자료 갱신' 패널이 `invoke refresh` 와 **같은 사슬**을 돌린다.
   - 사슬 정본을 `app/services/refresh_job.py` 의 `STEPS` 하나로 모았다 (`tasks.py` 는 부르기만)
   - 막는 것은 **환경이 아니라 능력**이다 — 배포본 · `REFRESH_API=off` · `scripts/` 부재 · 쓰기 불가
   - 경로는 **배포본에서도 등록**하고 실행만 `503`. 계약이 환경에 따라 갈리지 않게 한다
   - ⚠️ 남은 것: **워커가 하나라는 전제가 코드에 없다** · 로그 링버퍼 400줄 · OS 스케줄러는 그다음

**③ ADR-DS-0001 작성** — DART 클라이언트 표준
   `dart_report.py:36` 이 repositories 를 import 해 **계층을 역전시킨다.**
   ⚠️ DS-0001 은 **예약 번호**다. 미작성이지만 새 결정에 쓰지 않는다. 다음 새 번호는 **DS-0020** (DS-0015~0019 가 썼다).
   (ADR-DS-0003 `app/core/settings.py` 는 `f744238` 로 **완료**됐다.)

**④ 자료 보관 기능 — 절반 끝났다** (ADR-DS-0008 · **ADR-DS-0019**)
   ✅ **① 저장소+API 와 ③ 화면이 끝났다** (2026-08-25 · ADR-DS-0019).
   - `app/repositories/clip_store.py` — CRUD · URL 정규화 · 산업 자동유도 · `manual` 보호
   - `app/routers/clip_router.py` — 7연산. `kind` 별 검증은 여기 있다(`payload` 가 jsonb 라 표가 못 본다)
   - `/stock` 패널 — 담기 + 목록 + 필터(종류·연월·태그·전체보기) + 삭제
   - ⚠️ **Postgres 전용이라 배포본은 잠겨 있다.** 막는 것은 환경이 아니라 **능력**이고
     (`APP_ENV` 가 아니라 표에 붙어 보고 판단한다) S6 뒤에 고치지 않아도 살아난다

   ✅ **② 수집도 절반 끝났다 — DART 가 돈다** (2026-08-25 · **ADR-DS-0020**)
   - `app/services/dart_collector.py` 가 정본. 종목 하나는 `/stock` 버튼, 일괄은 `invoke collect`
   - **갱신 사슬 밖이다** — 시세(SQLite)와 보관함(Postgres)은 축이 다르고 순서 의존도 없다
   - ⚠️ **증분은 호출 수를 줄이지 않는다.** 줄이는 것은 재방문 억제(`REVISIT_HOURS=20`)뿐
   - ⚠️ **일괄이 사람 손에 달려 있다** — 화면 버튼도 스케줄러도 없다. 이 레포는 정확히
     그 종류의 고장을 겪었다(ADR-DS-0016 · 24거래일)
   - `securities.corp_code` 는 이제 **2,759종목**이다 — `build_corp_code.py` 가 `isdigit()` 로
     신형 코드(`0001A0`)를 빼고 있던 결함을 고쳤다

   ⚠️ **남은 것은 네이버 뉴스다**
   - **네이버 검색 API** — 키는 `.key` 의 `NAVER_API_CLIENT_ID`·`NAVER_API_SECRET_KEY` 에
     **배치 완료**. ⚠️ **일일 쿼터가 있으므로 호출 상한 설계가 먼저다**
   - 담기 버튼이 `/stock` 하나뿐이다. **두 번째 화면을 붙일 때** 공용 조각으로 뺀다 —
     지금 빼면 쓰는 곳이 하나뿐인 추상이 된다
   - 사이드바 '수집 자료' 다섯은 여전히 `soon`. 화면이 실제로 설 때 푼다
   - 메모·태그 수정 UI 가 없다(`PATCH` API 는 있다) · 산업을 직접 고르는 자리도 없다

**⑤ README 정체성 재작성** ✅ **끝났다** (2026-08-23)
   제목·리드·"무엇을 모으나"·"지금 어디까지 왔나"·30초 시작·목차를 새로 세웠고,
   §6 화면표에 빠져 있던 `/market`·`/timeseries` 를 넣고 준비중 5화면을 밝혔으며,
   §13 다음 단계를 실제 순서(S3→S4→S5→수집)로 갈아 끼웠다.
   본문 15장(인증키 배치·캐시·API 전수표·문제 해결·Vercel)은 **버리지 않고 그대로 살렸다**.
   ⚠️ **이미지만 남았다** — ①의 마지막 줄 참조.

**⑥ markdownlint 설정** ✅ **끝났다** (2026-08-23)
   `.markdownlint-cli2.jsonc` 로 **6,500건 → 0건.** 끈 것은 이 레포 규약과 충돌하는 6개
   (MD013 줄길이 · MD060 표 정렬 · MD036 · MD028 · MD033 · MD040)이고, **왜 끄는지를
   설정 파일에 적어 두었다.** 나머지 21건은 실제로 고쳤다.
   ⚠️ `lychee` 는 **여전히 없다.** `invoke docs-check` 가 폴백으로 **상대 링크만** 보고,
   앵커·외부 URL 은 못 본다는 것을 실행할 때마다 화면에 밝힌다.

**⑦ 상위 문서 일괄 정정** (세션 마무리에) — §8 참조

### 무효가 된 작업 — 하지 말 것

| 옛 지시 | 왜 무효인가 |
|---|---|
| "`/prices` 응답에 페이지네이션을 넣는다" | **`/prices` 는 존재하지 않는다**(전역 grep 0건). 미래 계약의 경로명이다. 실물 시계열 5경로에는 ADR-DS-0004 로 상한이 이미 섰다 |
| "`/export` 를 `APP_ENV=local` 전용으로 분리" | **뒤집혔다**(ADR-DS-0004 결정 3). 실물은 GIC 리포트 렌더러이고 배포본 화면이 호출한다. 막으면 기능이 죽는다. BI용 CSV 익스포트는 이 레포에 **아직 없다** — 만들 때 `/api/exports/*` 로 새 네임스페이스를 쓴다 |
| "엔트리포인트를 통일한다" | 완료 (ADR-DS-0006) |
| "Dockerfile·compose·tasks.py 를 만든다" | 완료. 잔여분은 `app/core/settings.py` 하나 |
| "`sql/init/` 에 DDL 을 작성한다" | 완료. 단 **저장계층 전환은 미착수** |

## 7. 완료 조건 (DoD)

- [x] `docker compose up -d` 후 `http://localhost:8000/docs` 에 Swagger 가 뜬다 (강사님 요건 1번)
- [x] `docker compose --profile local-db up -d` 로 DB 까지 뜨고 healthcheck 가 통과한다
- [x] `invoke check` 가 통과하고 `requirements.txt` 가 자동 생성된다 (강사님 요건 3번)
- [x] 시계열 5경로에 구간 상한 + 잘림 고지가 있다 (ADR-DS-0004)
- [x] `app/services/research/` 이관 경계표가 문서에 있다 (ADR-DS-0007)
- [x] 라우터 실제 개수와 목록이 문서와 일치한다 (17파일 · 엔드포인트 64개)
- [x] 이 서비스가 무엇인지 랜딩·README·`/guide`·OpenAPI 넷이 같은 문장으로 말한다 (ADR-DS-0012)
- [x] `invoke docs-check` 가 초록이다 (markdownlint 0건 · 상대 링크 31건 실재)
- [x] 문서가 Obsidian 볼트에 미러된다 (ADR-DS-0013 · `post-commit`)
- [ ] ADR-DS-0001 이 `docs/decisions/` 에 있다 (0003 은 rev.2 로 있다)
- [x] `app/core/settings.py` 가 있고 `APP_ENV` 분기가 코드에 실재한다
- [x] DB 엔진 계층이 있고 transaction 모드 풀러 뒤에서 실측됐다 (ADR-DS-0011 S2)
- [ ] 저장계층이 Postgres 로 전환됐다 (ADR-DS-0011 **S7~S9** — S1~S6 완료 · **로컬·배포본 읽기가 모두 Postgres**)
- [x] 자료 보관 스키마가 있다 (`clip` 표 · ADR-DS-0008)
- [~] 자료 보관이 **동작한다** — 담기·목록·필터·산업 보정은 `/stock` 에서 돈다 (ADR-DS-0019).
      **DART 자동 수집도 돈다** (ADR-DS-0020 · 한 회차 16,286건 · 표 전체 16,593건).
      **남은 것: 네이버 뉴스 + 다른 아홉 화면의 담기 버튼 + `/filings` 화면**
- [ ] README 에 썸네일·실행 스크린샷이 있다 (강사님 요건 4번) — **이 환경에 캡처 도구가 없다**

> ⚠️ `invoke check` 를 돌리면 `requirements.txt` 가 재생성돼 `git status` 에 M 으로 뜬다.
> 커밋 전 그 diff 가 **의도한 의존성 변경인지** 확인한다.

## 8. 정정해야 할 상위 문서 — 누적된 변경 노트

| 문서가 말하는 것 | 실물 |
|---|---|
| "라우터 7종" | **17파일** (API 15 + 화면 1 + `__init__`) · 엔드포인트 64개 |
| "클라이언트 5종" | **9종** (`dart_report`·`yf_data` 누락) |
| "`/prices` 페이지네이션 필요" | **`/prices` 는 존재하지 않는다.** 미래 계약의 경로명 |
| "`/export` 를 local 전용으로" | 실물은 GIC 리포트 렌더러. 막으면 배포본 기능이 죽는다 |
| "파일 캐시라 DB 가 없다" | SQLite 3개(172MB) + JSON + `/tmp` + 인메모리 4갈래 |
| "휴장일 계산 이미 구현됨" | `weekday() < 5` 로 주말만 거른다 |
| "이미 70% 완성" | 문서가 언급조차 않는 리서치 하네스 11,241행이 절반을 차지한다 |
| "상한이 없는 곳이 5곳" | **전부 상한이 섰다**(2026-08-17) |
| "2주짜리 팀 프로젝트 1개" | **2주 × 3개 = 6주 점진 확장** |
| "저장소 이름이 아직 api-test" | 개명 완료. GitLab 만 `est-` 접두사 |

대상: `docs/승격/module-readme-초안.md`(이 레포 안에 있다).
⚠️ `quant-contract`의 `CONTEXT.md`·`09-프로젝트-구조-재설계.md`도 원래 이 표의 정정
대상이었으나, 그 저장소 자체가 2026-09-15 폐기돼 더 이상 존재하지 않는다 — 그 두 문서는
정정할 필요가 없어졌다.

## 9. 이 레포에서 지킬 것

- 세션 시작 시 `AGENTS.md` 의 "이 모듈 특화" 절을 먼저 읽는다 — 공통 규칙과 한 단계 다른 지점이 있다
- 검증 정본은 `invoke check` 하나. venv 를 켜고 돌린다
- **clone 직후 `invoke hooks` 를 한 번 돌린다** — 문서를 Obsidian 볼트로 미러하는
  `post-commit` 훅이다 (ADR-DS-0013). 안 돌리면 볼트가 조용히 낡는다.
  `invoke preflight` 가 없으면 알려 주되 **막지는 않는다**
- `requirements.txt` 직접 편집 금지 (`uv export` 생성물)
- 커밋 메시지: `<scope>: <동사원형 요약>`, 한국어. main 직커밋, origin·gitlab 양쪽 push
- 문서·주석·대화는 한국어. 변수·함수명만 영어
- ⚠️ 이 레포는 서브모듈이지만 **읽기 전용이 아니다.** 읽기 전용은
  `stock-coin-trade` · `docker-class` · `investment-analysis` 뿐이다

## 10. 작업 마무리 의례 (사용자 지시)

한 작업이 끝날 때마다 **요청 없이 먼저** 두 가지를 한다.
1. **다음 세션 프롬프트를 답변 맨 끝에 복붙 가능한 형태로 준다.** 별도 파일을 만들지 않는다.
2. **docker 를 내린다** — `docker compose --profile local-db down` (테스트 데이터가 남았으면 `-v` 까지)

---

## 부록. 이 프로젝트의 컨벤션

- Python 3.12 / ruff (line-length 100) / 들여쓰기 2 spaces(설정 파일)·4 spaces(파이썬)
- 의존성 정본은 `pyproject.toml` + `uv.lock`. **`requirements.txt`는 `uv export` 생성물이므로 직접 편집 금지**
- 검증 정본은 `invoke check` 하나. Makefile·tox·check.py 를 따로 두지 않는다
- ADR은 `docs/decisions/NNNN-title.md`, 참조 키는 `ADR-DS-NNNN`. **번호 재사용 금지**
- 다이어그램은 Mermaid `flowchart` + `subgraph`. **`C4Context`/`C4Container` 문법 금지**(GitHub 미렌더링)
- 커밋 메시지: `<scope>: <동사원형 요약>` (예: `data-service: add clip table DDL`)
- 문서 문체: 개조식("-다"). 튜토리얼만 "-습니다"
- 모든 대화·주석·문서는 **한국어**. 변수·함수명만 영어
