# 세션 시작 프롬프트 — data-service

> 새 Claude Code 세션을 `projects\data-service` 에서 열고 **이 파일 전체를 첫 메시지로 붙여넣으세요.**
> 리서치 결과가 갱신되면 이 파일도 함께 갱신합니다.

---

## 0. 함께 첨부할 것 (순서대로)

1. `projects\quant-contract\CONTEXT.md` (rev.5)
2. `projects\quant-contract\research\01-data-service-검증완료.md` ← **확정 산출물이 그대로 있음**
3. 이 파일

## 1. 지금 여는 폴더

```
C:\Users\kik32\workspace\EST-Camp-AI-Quant\projects\data-service
```

✅ 이 폴더는 **git 서브모듈이자 독립 저장소**입니다. `api-test` 를 `git mv` 로 개명해
승격했으므로 히스토리와 원격(origin·gitlab)이 그대로 이어집니다.

⚠️ GitHub·GitLab 저장소 이름은 **아직 `api-test`** 일 수 있습니다. 호스트에서 이름을
바꾼 뒤 `git submodule sync --recursive` 와 `remote set-url` 을 실행해야 합니다.
절차: `../quant-contract/docs/explanation/legacy-to-module-map.md`

## 2. 이 모듈이 무엇인가

**국내 시장 데이터의 수집 · 정규화 · 조회 API.** 나머지 모듈이 전부 이걸 쓴다.

**이 레포는 ④ `api-test`를 개명해 승격한 것이다**(2026-08-16, `git mv`). 복사본이 아니라
같은 저장소이고 히스토리가 이어진다. **코드는 이미 여기 있다. 옮겨올 것이 없다.**

이미 70% 완성 상태다 — `app/clients/`(DART·KRX·ECOS·FRED·KOSIS·FSS·HF),
`app/core/trading_calendar.py`, `app/routers/` 7종, `app/repositories/` 파일 캐시.
**신규 개발이 아니라 계약(OpenAPI)을 정리하고 저장 계층을 붙이는 작업이다.**

## 3. 절대 제약 — 이걸 어기는 제안은 하지 말 것

| # | 제약 | 근거 |
|---|---|---|
| 1 | **LLM 유료 API 비용 0원** | 강사님 방침 |
| 2 | **매매 신호 생성 경로에 LLM 금지** | 비결정성 · 재현 불가 · 사전학습 룩어헤드 오염 |
| 3 | RAG·Agent는 신호 경로와 **물리적으로 분리된 별도 서비스** | 2번의 귀결 |
| 4 | AWS 등 클라우드 인프라 미사용 | 강사님 방침 |
| 5 | Docker 이미지 용량 최소화 | 로컬 디스크 제약 |
| 6 | **CI/CD를 필수 경로에 두지 않음** | GitHub 계정 리스크 |
| 7 | **헤비 프론트 프레임워크 금지** (Next.js·React SPA·번들러) | 강사님 방침. 빌드 단계가 생기면 6번도 흔들림 |

## 4. 이미 확정된 것 — 다시 논의하지 말 것

- **저장소 이원화** — 로컬 Postgres가 정본(full), Supabase는 데모용(core) (ADR-CT-0007)
- **유니버스 2단계** — `core` = KOSPI200+KOSDAQ150(약 350) / `full` = 전종목+상장폐지 (ADR-CT-0010)
- **OHLC는 `integer`**, `ohlcv`는 연 단위 RANGE 파티셔닝
- **엔트리포인트 정본 `app.main:app`**, Vercel은 `[tool.vercel] entrypoint`로 별도 지정
- **`pgvector/pgvector:0.8.2-pg16`** (CVE-2026-3172 패치본). `pg16` 롤링 태그 금지
- **DB 컨테이너는 compose profile `local-db` 뒤**에 둔다. 기본 up에서는 안 뜬다
- **의존성 정본은 `pyproject.toml` + `uv.lock`.** `requirements.txt`는 `uv export` 생성물
- Vercel 실행시간은 Fluid compute **전 플랜 300초**(Hobby 10초는 낡은 정보),
  요청·응답 본문 **4.5MB 한도**

## 5. legacy — 읽기 전용

### 승격된 것 — 더 이상 legacy 가 아니다 (2026-08-16)

이 둘은 `upstream`(강사님 원본) 원격이 없어 3-way merge 계보 부담이 없다.
그래서 **복사하지 않고 폴더를 개명해 그 자리에서 발전시킨다.** 히스토리가 이어진다.

| 기호 | 이전 이름 | 현재 |
|---|---|---|
| ④ | `api-test` | **`projects/data-service`** |
| ① | `investment-portfolio-site` | **`projects/dashboard`** |

### 읽기 전용 — 수정 금지

코드를 옮길 때는 이 모듈 안에 **복사한 뒤** 수정한다.

| 기호 | 경로 | 스택 | 계보 |
|---|---|---|---|
| ② | `C:\Users\kik32\workspace\Research-Prompt-Engineering` | Python | **projects 밖!** |
| ③ | `projects/stock-coin-trade` | Django 5.2.17 + DRF 3.18 + HTMX/Alpine | `upstream` + `upstream-main` |
| ⑤ | `projects/docker-class` | Docker·DevSecOps 실습 | `upstream` + `upstream-main` |
| — | `projects/investment-analysis` | 강의 원본 | `upstream` |

**③⑤와 investment-analysis 는 폴더를 이동·개명하지 않는다.** 강사님 원본과 3-way merge
계보가 살아 있어서(`upstream-main` 브랜치), 파일을 대거 이동한 뒤 upstream 을 얹으면
병합이 깨진다. ③은 추가로 **체결 리팩터링의 회귀 확인 대상**이라 계속 돌아가야 한다.

각 레포의 `NOTICE.md`(원저작자 edumgt 표시)는 승격 후에도 유지한다.

## 6. 이번 세션의 목표

**작업 0 (30m).** 승격 정리 — 루트 `README.md`를 `docs/승격/module-readme-초안.md`
내용으로 교체(기존 내용 중 살릴 것 흡수), `LICENSE`(MIT) 원문 확인,
`lecture/` 처리 방침 결정.

**작업 1 (1h).** 엔트리포인트 통일 — 현재 루트 `main.py`와 `app/` 이 공존한다.
정본을 **`app.main:app`** 으로 맞추고 `pyproject.toml`의 `[tool.vercel] entrypoint`도 함께.

**작업 2 (2h).** `01-data-service-검증완료.md` §5의 확정 산출물 반영
— `docker-compose.yml`(§5.1) · `Dockerfile`(§5.2) · `.dockerignore`(§5.3)
· `app/core/settings.py`(§5.4) · `tasks.py`. 기존 `requirements.txt`는
`pyproject.toml` + `uv.lock`으로 옮기고 생성물로 돌린다.

**작업 3 (2h).** `sql/init/`에 DDL 작성 (§7의 뼈대: `ohlcv` 파티셔닝 · `watermark` ·
`securities.universe_tier`). 지금은 `app/repositories/*`가 파일 캐시라 DB가 없다.

**작업 4 (2h).** 기존 라우터 7종의 응답을 계약에 맞춤 — 페이지네이션 추가,
`/export`를 `APP_ENV=local` 전용으로 분리, `/docs`(Swagger) 확인.

**작업 5 (1h).** `app/services/timeseries/*`를 `factor-service`로 이관할 준비
(지금 지우지 말 것. factor-service 착수 시 복사).

## 7. 완료 조건 (DoD)

- [ ] `docker compose up -d` 후 `http://localhost:8000/docs`에 Swagger가 뜬다 (강사님 요건 1번)
- [ ] `docker compose --profile local-db up -d`로 DB까지 뜨고 healthcheck가 통과한다
- [ ] `invoke check`가 통과하고 `requirements.txt`가 자동 생성된다 (강사님 요건 3번)
- [ ] `/prices` 응답에 페이지네이션이 있다 (4.5MB 한도)
- [ ] `/export`는 `APP_ENV=local`에서만 등록된다
- [ ] ADR-DS-0001 ~ 0004가 `docs/decisions/`에 있다
- [ ] README에 썸네일·실행 스크린샷이 있다 (강사님 요건 4번)

## 8. 첫 지시

이 레포는 api-test 를 개명한 것이라 **코드가 이미 여기 있다. 복사할 것이 없다.**

먼저 루트 `main.py`와 `app/` 의 관계, `app/routers/` 7종이 무엇을 노출하는지,
`requirements.txt`와 `vercel.json`의 현재 설정을 읽고 —
**`app.main:app` 엔트리포인트로 통일하는 재배치안**을 파일 단위로 제시해줘.

그 다음 `app/repositories/*`(파일 캐시)를 Postgres watermark 로 바꾸는 경계표를 만들어줘.

⚠️ 이 레포는 서브모듈이지만 **읽기 전용이 아니다.** 여기가 작업 대상이다.
   읽기 전용은 `stock-coin-trade` · `docker-class` · `investment-analysis` 뿐이다.

---

## 부록. 이 프로젝트의 컨벤션

- Python 3.12 / ruff (line-length 100) / 들여쓰기 2 spaces(설정 파일)·4 spaces(파이썬)
- 의존성 정본은 `pyproject.toml` + `uv.lock`. **`requirements.txt`는 `uv export` 생성물이므로 직접 편집 금지**
- 검증 정본은 `invoke check` 하나. Makefile·tox·check.py 를 따로 두지 않는다
- ADR은 `docs/decisions/NNNN-title.md`, 참조 키는 `ADR-{스코프}-{번호}`
- 다이어그램은 Mermaid `flowchart` + `subgraph`. **`C4Context`/`C4Container` 문법 금지**(GitHub 미렌더링)
- 커밋 메시지: `<scope>: <동사원형 요약>` (예: `factor-service: add available_at column`)
- 문서 문체: 개조식("-다"). 튜토리얼만 "-습니다"
- 모든 대화·주석·문서는 **한국어**. 변수·함수명만 영어
